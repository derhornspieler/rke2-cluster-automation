apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
  labels:
    app: deepflow-mysql
spec:
  type: ClusterIP
  selector:
    app: deepflow-mysql
  ports:
  - port: 3306
    targetPort: mysql
    name: mysql
